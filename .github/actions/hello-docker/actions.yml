name: Hello World
author: Murat Yarali
description: Some description
inputs:
  who-to-greet:
    description: "Who to greet"
    required: true
    default: Murat
outputs:
  time:
    description: "The greeting time"
runs:
  using: "docker"
  image: "Dockerfile"
  # entrypoint:
  args:
    - ${{ inputs.who-to-greet }}